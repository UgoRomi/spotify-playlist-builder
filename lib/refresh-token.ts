export function refreshToken() {
  /**
   * TODO: call the refresh-token API endpoint
   * If the refresh token is valid, the API will return a new access token and we update the cookie
   * If the refresh token is invalid, we clear the cookies and redirect to the login page
   */
}
